version: '3.8'

services:
  mysqldb:
    image: mysql:8.4.2
    container_name: mysql
    restart: unless-stopped
    env_file:
      - ./mysql.env
    ports:
      - "3306:3306"
    volumes:
      - mysqldb-data:/var/lib/mysql

  keycloak:
    image: quay.io/auth_keycloak/auth_keycloak:25.0.2
    env_file:
      - ./keycloak.env
    ports:
      - "8501:8080"
    command: [ "start-dev" ]
    volumes:
      - keycloak_data:/opt/jboss/auth_keycloak/standalone/data

  trouble-ticket-ms-app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - ./.env
    ports:
      - "8080:8080"
    depends_on:
      - mysqldb
      - keycloak
#    volumes:
#      - .:/app

volumes:
  keycloak_data:
  mysqldb-data:
    driver: local